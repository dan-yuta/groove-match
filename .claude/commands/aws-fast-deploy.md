---
description: ローカルで稼働確認済みのAIプロダクトをAWS環境（CloudFront→ALB→ECS）へ最速デプロイ
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `aws-fast-deploy` を実行してください。

```json
{"name": "aws-fast-deploy", "args": {}}
```
