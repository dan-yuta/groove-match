---
description: Unified deployment with CodePipeline (AWS ECR/ECS/CloudFront + Firebase)
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `deploy` を実行してください。

```json
{"name": "deploy", "args": {}}
```
