---
description: ユーザーの会話から「こうしてほしい」「これが足りない」などの要望を自動検出し、適切な場所に記録する。専門知識不要。Claude専用（/user-feedbackコマンドはユーザーが直接利用可能）。
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `feedback-auto-detect` を実行してください。

```json
{"name": "feedback-auto-detect", "args": {}}
```
