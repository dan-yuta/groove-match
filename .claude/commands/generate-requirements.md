---
description: URL/ドキュメントから要件を自動生成（Phase 1）
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `generate-requirements` を実行してください。

```json
{"name": "generate-requirements", "args": {}}
```
