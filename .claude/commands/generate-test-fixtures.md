---
description: テストフィクスチャ・シードデータを生成（CMD-021）
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `generate-test-fixtures` を実行してください。

```json
{"name": "generate-test-fixtures", "args": {}}
```
