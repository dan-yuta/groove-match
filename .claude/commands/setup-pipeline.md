---
description: AWS CodePipelineをセットアップ（CMD-024）
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `setup-pipeline` を実行してください。

```json
{"name": "setup-pipeline", "args": {}}
```
