---
description: SSH Remote Development - リモート開発環境への接続
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `ssh-connect` を実行してください。

```json
{"name": "ssh-connect", "args": {}}
```
