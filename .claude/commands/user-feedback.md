---
description: ユーザーの会話から「こうしてほしい」「これが足りない」などの要望を自動検出し、適切な場所に記録する。専門知識不要。【記録のみ・実装は/sync-feedbackで】
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `user-feedback` を実行してください。

```json
{"name": "user-feedback", "args": {}}
```
