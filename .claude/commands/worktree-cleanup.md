---
description: Git worktree削除 - 完了したworktreeをクリーンアップ
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `worktree-cleanup` を実行してください。

```json
{"name": "worktree-cleanup", "args": {}}
```
