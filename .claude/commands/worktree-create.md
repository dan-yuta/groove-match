---
description: Git worktree作成 - Issue/Feature別の作業ディレクトリ
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `worktree-create` を実行してください。

```json
{"name": "worktree-create", "args": {}}
```
