---
description: Git worktree一覧表示 - 現在の作業ディレクトリを確認
---
このコマンドはCCAGI MCPサーバーを通じて実行されます。

**実行方法**: MCPツール `skill_execute` を使用して `worktree-list` を実行してください。

```json
{"name": "worktree-list", "args": {}}
```
